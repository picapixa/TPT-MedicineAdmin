<Page
    x:Class="TPT_MMAS.View.IoT.IotShell"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TPT_MMAS.View.IoT"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    DataContext="{Binding IotShell, Source={StaticResource Locator}}">
    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="80" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <RelativePanel Background="Black">
            <TextBlock x:ConnectionId='2' x:Name="tb_time" Text="{Binding CurrentTime, Converter={StaticResource DateTimeConverter}}" 
                       Foreground="White" FontSize="20"
                       Margin="20.6,0.6,19.2,-0.4"
                       RelativePanel.LeftOf="btn_user"
                       RelativePanel.AlignVerticalCenterWithPanel="True"/>
            <Button x:ConnectionId='3' x:Name="btn_user" Margin="0,0,20,0"
                     RelativePanel.AlignRightWithPanel="True"
                     RelativePanel.AlignVerticalCenterWithPanel="True">
                <Button.Content>
                    <Ellipse Width="40" Height="40" Fill="Blue"/>
                </Button.Content>
            </Button>
        </RelativePanel>
        <Frame x:ConnectionId='1' x:Name="iotShellFrame" Grid.Row="1"                               />
    </Grid>
</Page>

