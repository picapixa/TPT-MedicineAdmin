<Page
    x:Class="TPT_MMAS.Iot.Shell"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TPT_MMAS.Iot"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    DataContext="{Binding Shell, Source={StaticResource Locator}}">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="88" />
            <RowDefinition />
        </Grid.RowDefinitions>

        <Grid x:ConnectionId='1' x:Name="TopBar" Background="Black">
            <StackPanel Padding="20,12">
                <TextBlock Text="{Binding CurrentDateTime.Now.TimeOfDay, Mode=OneWay}" Foreground="White" FontSize="28"
                       VerticalAlignment="Center" HorizontalAlignment="Right"/>
                <TextBlock Text="{Binding CurrentDateTime.Date, Mode=OneWay}" Foreground="White" FontSize="18"
                       VerticalAlignment="Center" HorizontalAlignment="Right"/>
            </StackPanel>
        </Grid>
        <Frame x:ConnectionId='2' Grid.Row="1" x:Name="shellFrame"                            />
    </Grid>
</Page>

