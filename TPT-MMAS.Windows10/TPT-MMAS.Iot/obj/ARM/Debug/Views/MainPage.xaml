<Page x:ConnectionId='1'
    x:Class="TPT_MMAS.Iot.Views.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TPT_MMAS.Iot.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    DataContext="{Binding Main, Source={StaticResource Locator}}">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="320" />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>

        <Grid Background="Crimson" Padding="20">
            <StackPanel VerticalAlignment="Center">
                <TextBlock x:ConnectionId='3' x:Name="tb_ipadd"                                                                                Foreground="White" FontSize="20" />
                <TextBlock x:ConnectionId='4' x:Name="tb_devname"                                                                         Foreground="White" FontSize="32"/>
            </StackPanel>
        </Grid>

        <Grid Grid.Column="1">
            <StackPanel Padding="40" VerticalAlignment="Center">
                <TextBlock Style="{ThemeResource HeaderTextBlockStyle}"
                           Margin="0,0,0,32" FontSize="32">
                    Welcome to the Mobile Medicine Administration System
                </TextBlock>
                <TextBlock Style="{ThemeResource BaseTextBlockStyle}" FontSize="20" FontWeight="SemiLight">
                    Please login at the In-patient Management System to use this device.
                </TextBlock>

                <StackPanel Margin="0,20" Orientation="Horizontal">
                    <Button IsEnabled="False">Connect to Wi-Fi</Button>
                    <Button x:ConnectionId='2' Margin="12,0,0,0"                         >Debugging</Button>
                </StackPanel>
            </StackPanel>
        </Grid>

    </Grid>
</Page>

